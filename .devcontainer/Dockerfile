FROM localhost/claude-devcontainer:latest

# Add project-specific setup below. For example:
# COPY start-servers.sh /usr/local/bin/
# USER root
# RUN chmod +x /usr/local/bin/start-servers.sh
# USER node
