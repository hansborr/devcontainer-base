# Copy to .env (gitignored) and fill in. The .env file is read by dolt.container
# via EnvironmentFile=. Deliver the sync-user password to clients out-of-band.
DOLT_ROOT_PASSWORD=replace-with-a-long-random-root-password
