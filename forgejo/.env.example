# Copy to forgejo/.env (gitignored). Consumed by forgejo-backup.sh.
# Where nightly `forgejo dump` archives are rsync'd over the tailnet, and how many to keep.
AURA_HOST=aura-farming.tail76c33c.ts.net
AURA_USER=dev
REMOTE_DIR=forgejo-backups
RETENTION=14
